import { MaxRangeDirective } from './max-range.directive';

describe('MaxRangeDirective', () => {
  it('should create an instance', () => {
    const directive = new MaxRangeDirective();
    expect(directive).toBeTruthy();
  });
});
